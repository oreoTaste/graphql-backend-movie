type Movie {
    id: Int!
    title: String!
    rating: Float!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    deleteOne(id: Int!): Boolean!
    addMovie(title: String!, rating: Float!): Movie!
}