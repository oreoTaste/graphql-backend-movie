type Movie {
    id: Int!
    name: String!
    score: Int!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    deleteOne(id: Int!): Boolean!
    addMovie(name: String!, score: Int!): Movie!
}